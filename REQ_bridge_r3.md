# REQ_bridge_r3.md — R3 轮：agent 使用体验深化

基线：/tmp/bridge_repo @ da7680d（0.7.0，R2 已完成工具描述重写 + 0.1.5 适配）
约束：src/index.ts 约 2800 行，先 grep/read 精确定位再改，禁止整文件重写。所有改动只落本仓库。

## 背景
R2 已把 26 个工具的 description 面向 agent 重写。本轮从「agent 实际调用轨迹」出发深化体验：
即模拟一个新 agent 首次接入本桥的真实路径，找出仍在踩的坑。

## 任务清单

### A. 返回结构精简（降低 agent 上下文负担）
1. 审计所有工具的返回 JSON：
   - 冗余字段（与请求回显重复的参数、内部实现细节字段）删除或收敛。
   - 列表类返回（session_list/task_list/approval_list）超过 20 条时截断 + 返回 `total` 与提示「用 offset/limit 参数」；若工具不支持分页参数则补上分页参数。
   - session_log 默认返回条数设上限（如 50 条事件），超限时返回首尾 + `truncated: true` + 如何取更多的提示。
2. 时间戳统一输出人类可读格式（ISO8601 本地时区），保留原始 epoch 于 `*_at_epoch` 字段（若调用方需要排序）。
3. 字节/时长等数值带单位或格式化（如 `8.8s`、`9.4KB`），原始值保留。

### B. agent 工作流指引（agent_run / task_inbox 深化）
4. agent_run 返回中，若任务产出提及「已写入文件」，检查结果里是否带文件绝对路径；agent 结果文本若无路径但沙箱 cwd 有新增文件，附 `hint` 提示常见落点（沙箱 cwd）。
5. task_inbox 提交后返回 `taskId` 的同时附预计保留时长与轮询建议（已有则确认文案一致性）。
6. 审批流（approval_list/approval_respond）：返回中给一处「当前挂起审批数」汇总，agent 不用反复轮询列表才知道有没有审批。

### C. 一致性与防御
7. 所有工具对「必传参数缺失」「id 不存在」「会话为空」三类错误的文案格式统一为：
   `<错误>: <关键值> (<原因一句话>; 下一步动作)`。
8. 参数校验前移：在工具入口统一校验类型/必填，报错信息指明期望类型与实际收到什么（如 `cwd: expected string, got number`）。
9. 检查所有工具在「dsh 服务未启动/连接拒绝」时的错误文案，统一给出「检查 dsh.service 状态」指引。

## 验收
- 为 A/B/C 每组改动补单测（mock 实例模式），全部通过。
- `npx tsc --noEmit` 零错误。
- 版本号不动（仍 0.7.0，R5 统一定版）；CHANGELOG.md 追加到 0.7.0 段落。
- 最后输出 A/B/C 分组修改摘要（文件:行号）+ 测试真实输出。
- 提交 git commit（作者用仓库既有 git 身份）。
